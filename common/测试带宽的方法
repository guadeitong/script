机器A，启动nc监听端口
nc -l -n 1234|wc -c
机器B，启动nc一直发送数据
time yes | nc -n A_IP 1234 
一段时间后，在机器B上按下Ctrl+C，用A机器上输出的数字和B机器上输出的时间算出带宽就行了。比如我这里，A上输出 30496256，B上输出
real    0m1.080s
user    0m0.804s
sys     0m0.699s
带宽是 30496256 / 1.080 bps